package com.prompthavenai.falcibuket.data.model

/** How a fortune type collects its input from the user. */
enum class InputMode {
    PHOTO_VISION,
    CARD_SELECTION,
    TEXT_INPUT,
    BIRTH_DATA,
    TWO_PERSON_DATA,
    SYMBOL_SELECTION,
    RANDOM_CAST,
    QUESTION_ONLY
}
